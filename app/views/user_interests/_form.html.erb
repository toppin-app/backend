<%= form_with(model: user_interest, local: true) do |form| %>
  <% if user_interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_interest.errors.count, "error") %> prohibited this user_interest from being saved:</h2>

      <ul>
        <% user_interest.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :interest_id %>
    <%= form.text_field :interest_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
