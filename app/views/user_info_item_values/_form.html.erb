<%= form_with(model: user_info_item_value, local: true) do |form| %>
  <% if user_info_item_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_info_item_value.errors.count, "error") %> prohibited this user_info_item_value from being saved:</h2>

      <ul>
        <% user_info_item_value.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :info_item_value_id %>
    <%= form.text_field :info_item_value_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
