# Toppin persistent work log

Before starting any task in this repository, read `../AGENTS.md` and consult
the canonical Toppin database in Notion. Before the final response for any
material task, record the work in that database following the shared
instructions.

After validated server changes are committed, push them to the configured
remote unless the user explicitly asks not to push.
